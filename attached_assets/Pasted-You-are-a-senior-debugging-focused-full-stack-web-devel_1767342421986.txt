You are a senior debugging-focused full-stack web developer.

I have already built a web app for a Personal Growth & Career OS,
but it is NOT working correctly.

Your task is to:
- Identify ALL errors
- Fix broken logic
- Fix navigation issues
- Fix access flow
- Improve UX
- Rebuild it into a STABLE, CLEAN, FULLY WORKING version

====================================================
CRITICAL OBJECTIVES
====================================================
1. The app MUST work end-to-end with ZERO errors
2. No broken buttons
3. No blank screens
4. No stuck states
5. No console errors
6. Smooth, predictable flow
7. Mobile-friendly
8. Event-safe (can be used repeatedly)

====================================================
TECH CONSTRAINTS (DO NOT BREAK)
====================================================
- Use ONLY HTML, CSS, Vanilla JavaScript
- NO frameworks
- NO backend
- Use localStorage safely
- Defensive coding (null checks everywhere)
- Clear comments
- Simple logic
- One responsibility per function

====================================================
CORE FLOW (MUST WORK PERFECTLY)
====================================================
1. User enters a valid access code
2. Code is verified correctly
3. User enters the system
4. Full-screen guided experience starts
5. Questions load one-by-one
6. Answers are saved safely
7. Result is generated correctly
8. Code is marked as USED
9. Reuse is BLOCKED
10. Session ends cleanly

====================================================
ERROR HANDLING (MANDATORY)
====================================================
- If localStorage data is missing → handle gracefully
- If user refreshes page → recover state or show message
- If invalid code → clear error message
- If code already used → block access
- If form incomplete → show guidance
- Never crash or freeze

====================================================
UI & UX IMPROVEMENTS
====================================================
- Full-screen layout (100vh)
- Clear headings
- One action per screen
- Disable buttons until valid input
- Show progress indicator
- Loading screen before result
- Clean color theme
  Primary: #009688
  Secondary: #E53935
  Background: #FFFFFF
  Text: #1E1E1E

====================================================
STRUCTURE (REBUILD IF NEEDED)
====================================================
/index.html      → Access code entry
/questions.html  → Guided question flow
/result.html     → Final output
/style.css       → Shared styles
/script.js       → ALL logic (clean & commented)

You may REMOVE unnecessary files and SIMPLIFY
if it improves stability.

====================================================
DEBUGGING TASKS
====================================================
- Inspect all JS logic
- Fix event listeners
- Fix navigation bugs
- Fix missing DOM elements
- Fix wrong IDs / classes
- Fix localStorage key mismatches
- Fix conditional logic
- Fix state handling

====================================================
FINAL REQUIREMENT
====================================================
After fixing:
- Test entire flow logically
- Ensure ZERO console errors
- Ensure consistent behavior
- Ensure professional UX

Return ONLY the corrected, improved, fully working code.
Do NOT explain.
Do NOT summarize.
Only return production-ready files.
